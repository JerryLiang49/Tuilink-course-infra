{
  "name": "tuilink-project-infra",
  "version": "0.0.1",
  "license": "MIT",
  "bin": {
    "tuilink-project-infra": "bin/tuilink-project-infra.js"
  },
  "scripts": {
    "bootstrap:staging": "cross-env NODE_ENV=staging cdk bootstrap --profile staging --region us-east-1",
    "bootstrap:prod": "cross-env NODE_ENV=prod cdk bootstrap --profile prod --region us-east-1",
    "deploy": "cdk deploy --require-approval never",
    "deploy-ai-auto-reply:staging": "cross-env NODE_ENV=staging yarn deploy --app 'ts-node bin/ai-auto-reply.ts' --profile staging",
    "deploy-ai-auto-reply:prod": "cross-env NODE_ENV=prod yarn deploy --app 'ts-node bin/ai-auto-reply.ts' --profile prod",
    "deploy-jd-resume-matcher:staging": "cross-env NODE_ENV=staging yarn deploy --app 'ts-node bin/jd-resume-matcher.ts' --profile staging",
    "deploy-jd-resume-matcher:prod": "cross-env NODE_ENV=prod yarn deploy --app 'ts-node bin/jd-resume-matcher.ts' --profile prod"
  },
  "devDependencies": {
    "@types/node": "22.7.9",
    "aws-cdk": "^2.1020.2",
    "cross-env": "^7.0.3",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.3"
  },
  "dependencies": {
    "aws-cdk-lib": "2.204.0",
    "constructs": "^10.4.2",
    "dotenv": "^16.5.0"
  }
}
